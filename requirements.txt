fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.2
# Supabase Python client — DB queries (PostgREST) + Storage
supabase==2.5.1
# python-dateutil required by analytics tracker to parse Supabase ISO timestamps
python-dateutil==2.9.0
# anthropic: sync client wrapped in asyncio.to_thread() in core/llm.py
# Upgrade to >=0.50 for native async client support
anthropic==0.34.2
qdrant-client==1.9.1
pymupdf==1.24.10
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
python-dotenv==1.0.1
sse-starlette==2.1.0
# chromadb kept for the ONNX embedding model (ONNXMiniLM_L6_V2) used in qdrant_store.py
chromadb==0.5.5
